# sms module
<p> This is a simple sms module that can be used to send sms to users </p>

```bash
npm install --save africastalking
npm install --save dotenv
npm install --save express
```

to run the server
```bash
node server.js
```

